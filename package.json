{
  "name": "stellar-card-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Root package for the StellarCard NFT game (contracts + frontend)",
  "author": "Your Name",
  "license": "ISC",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "preview": "npm run preview -w frontend",
    "test:contract": "sh ./scripts/test_contracts.sh",
    "build:contract": "cd contracts && stellar contract build",
    "deploy:contract": "scripts/deploy_contract.bat"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^29.0.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "tslib": "^2.8.1"
  }
}